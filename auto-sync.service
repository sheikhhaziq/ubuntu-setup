[Unit]
Description=Auto commit and push dotfiles every 30 minutes

[Service]
Type=oneshot
ExecStart=/bin/bash -c '
cd ~/ubuntu-setup || exit 0
bash dump-packages.sh
git add -A
git commit -m "auto-sync $(date)" && git push || true
'
